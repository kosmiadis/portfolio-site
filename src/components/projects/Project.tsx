import type { ProjectI } from "@data/data";

export default function Project ({title, description, featuredImg, github, to, techStack, tags, images}: ProjectI) {

    return <div className="w-full">
        
    </div>
}